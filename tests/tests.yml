- hosts: localhost
  roles:
  - role: standard-test-basic
    tags:
      - classic
    required_packages:
      - llvm-test
    tests:
      - regression-tests:
          dir: ./
          run: /usr/libexec/tests/llvm/run-lit-tests --threads 1
